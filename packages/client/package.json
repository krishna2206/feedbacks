{
  "name": "@feedbacks/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "description": "Typed, dependency-free client of the Feedbacks REST API v1 (used by the CLI and the MCP server)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "echo 'client: source-only package (bundled by the CLI and the MCP server)'"
  },
  "devDependencies": {
    "@types/node": "^22.20.4"
  }
}
